name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  image_picker: ^0.8.5+3 
  path_provider: ^2.0.11 
  google_mlkit_commons: any 
  google_mlkit_face_detection: ^0.2.0 # 얼굴 인식 (최신은 0.11.1인데.. 함수 하나가 없어서 다운그레이드함)



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
